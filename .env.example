# ==============================================================================
# Raindrop.io OAuth (required)
# ==============================================================================
# 1. Go to: https://app.raindrop.io/settings/integrations
# 2. Create a new app
# 3. Set Redirect URI to: http://localhost:3000/callback
# 4. Copy Client ID and Client Secret below

RAINDROP_CLIENT_ID=your_client_id
RAINDROP_CLIENT_SECRET=your_client_secret
RAINDROP_REDIRECT_URI=http://localhost:3000/callback

# ==============================================================================
# X.com OAuth (optional - for Twitter bookmark import)
# ==============================================================================
# 1. Go to: https://developer.twitter.com/en/portal/dashboard
# 2. Create a project and app with OAuth 2.0
# 3. Set Redirect URI to: http://localhost:3001/callback
# 4. Enable scopes: tweet.read, users.read, bookmark.read, offline.access
# 5. Copy Client ID and Client Secret below

X_CLIENT_ID=your_x_client_id
X_CLIENT_SECRET=your_x_client_secret
X_REDIRECT_URI=http://localhost:3001/callback
